var config = {};

// CycleStreets API base URL (not slash-terminated) and key
config.apiKey = 'cyclestreets_key';

// Mapbox API key
config.mapboxApiKey = 'mapbox_key';

// Google Maps API key (for Street View)
config.gmapApiKey = 'google_key';

